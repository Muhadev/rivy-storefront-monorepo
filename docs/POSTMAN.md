# Postman Usage

- Import `docs/postman_collection.json` into Postman.
- Set the `baseUrl` variable (default: `http://localhost:4000/api`).
- The collection covers: health, products, cart, checkout, orders, auth.
